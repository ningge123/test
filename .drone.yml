---
kind: pipeline
type: docker
name: drone-staging-production
steps:
  - name: build
    image: www.cocoyo.xin/drone-plugins/plugins/docker
    pull: always
    settings:
      tags: latest
      repo: www.cocoyo.xin/cc/test
      username: jnp
      password: Harbor123456
      insecure: true
    volumes:
        - name: docker
          path: /var/run/docker.sock
trigger:
  branch:
    - master
  event:
    - push
    - pull_request
